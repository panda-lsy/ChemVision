name = "chemvision-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# 部署后 URL 格式：https://chemvision-proxy.<your-subdomain>.workers.dev
# 如需自定义域名，在 Cloudflare Dashboard 中绑定

[vars]
# 可选：限制允许的来源域名（留空则允许所有来源）
# ALLOWED_ORIGINS = "https://<username>.github.io"
